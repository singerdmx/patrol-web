<%= form_for(@check_result) do |f| %>
  <% if @check_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@check_result.errors.count, "error") %> prohibited this check_result from being saved:</h2>

      <ul>
      <% @check_result.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <p>
      <%= f.label :result %><br>
      <%= f.text_field :result %>
    </p>

    <p>
      <%= f.label :session %><br>
      <%= f.text_field :session %>
    </p>

    <p>
      <%= f.label :check_route_id %><br>
      <%= f.text_field :check_route_id %>
    </p>

    <p>
      <%= f.label :check_point_id %><br>
      <%= f.text_field :check_point_id %>
    </p>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
